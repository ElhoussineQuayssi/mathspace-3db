# TODO

## Spatial quiz expansion
- [x] Inspect current spatial quiz implementation and data source.
- [ ] Add multiple additional `type: "spatial"` questions in `src/lib/quiz-data.ts`.
- [ ] Include additional shapes supported by `UniversalNetScene` (`cube`, `pyramid`, `cylinder`, `cone`) while keeping type safety (current `SpatialDiagnostic` only supports `cube`).
- [ ] Run `npm run lint` and `npm run build` to verify TypeScript correctness.

